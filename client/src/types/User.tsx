// this is the same as profile type right now, but we may want to separate them later
export type User = {
  id: number;
  email: string;
  display_name: string;
  bio: string;
  location: string;
  url: string;
  username: string;
  avatar_url: string;
  created_at: string;
};
